## 1. State

- [x] 1.1 Add `showSecret` boolean state to `UploadPage`

## 2. UI

- [x] 2.1 Wrap secret `<input>` in a relative-positioned `<div>`
- [x] 2.2 Switch input `type` between `"password"` and `"text"` based on `showSecret`
- [x] 2.3 Add absolute-positioned toggle `<button>` with eye / eye-slash SVG icons
- [x] 2.4 Wire button `onClick` to toggle `showSecret`
